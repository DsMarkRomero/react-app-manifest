apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
  namespace: ns-react-app-master
  labels:
    app: react-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-container
          image: localhost:32000/prime-number-frontend:master-6870e3c4b03bf2eaf22a42e057f64383ae189309
          imagePullPolicy: Always
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: regcred
