apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
  namespace: ns-react-app-master
  labels:
    app: react-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-container
          image: localhost:32000/prime-number-frontend:master-d329a31c8132e957cf0a21892a1b3498951ad515
          imagePullPolicy: Always
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: regcred
