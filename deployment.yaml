apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
  namespace: ns-react
  labels:
    app: react-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-container
          image: localhost:32000/prime-number-frontend:1ff238a68bf4cadc54cda76853351c4db9942809
          imagePullPolicy: Always
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: regcred
